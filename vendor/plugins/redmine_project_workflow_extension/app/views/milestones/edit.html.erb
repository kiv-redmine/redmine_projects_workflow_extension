<h2><%=l(:label_milestone)%></h2>

<% labelled_tabular_form_for @milestone, :url => project_milestone_path(@project) do |f| %>
<%= render :partial => 'form', :locals => { :f => f } %>
<%= submit_tag l(:button_save) %>
<% end %>

